{
  "name": "@uitulkit/card",
  "description": "Card component from @uitulkit library",
  "respository": "https://github.com/inavac182/uitulkit",
  "license": "MIT",
  "version": "0.1.0",
  "private": false,
  "files": [
    "dist"
  ],
  "main": "dist/index",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": " rm -rf dist node_modules",
    "tsc": "tsc",
    "watch": "tsc --watch",
    "eslint": "eslint ./src/**/*.{ts,tsx} --fix",
    "lint:styles": "stylelint ./src/**/*.tsx",
    "lint": "npm run lint:styles && npm run tsc && npm run eslint",
    "test": "jest"
  },
  "dependencies": {},
  "peerDependencies": {
    "react": ">= 16.8.0 < 17",
    "react-dom": ">= 16.8.0 < 17"
  }
}
